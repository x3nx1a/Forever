#include "StdAfx.hpp"
#include "File.hpp"

namespace FileLoader
{
	void handle(File::LoadData& file)
	{
		if (file.ext == "json")
		{
			if (file.baseName == "characters")
			{

			}
		}
	}
}